DON'T implement any code, just offer a system design doc (saving in prompt dir) for the `chat gpt PDF` system:


(keep things simple and elegant)


1. FE + BE app
2. FE: vue 3
3. BE: fast api + LLM (gpt, gemini, claude ...) + SQLITE + RAG db
	- use `uv` as python pkg manager
	- use .env save API keys
4. the system needs to be able to load multiple pdf, transform the
   PDF data into RAG (use local DB, the simple one), and answer the questions based on the knowledge base

FE, BE app interact each other with HTTP, and long-lived connections(web socket, TCP, ..)

5. need to record the history chat, so we can view the dialogue, reload them
   and continue asking LLM questios

6. feel free to use agentic framework: e.g. crewAI, langgraph ...
7. the gen docs need to cover (but not limited to) below:

 - system architecture
 - core module design
 - API design
 - data model (db schema)
 - flow diagram
 - implement plan / phase
 - concerns, things to improve ... 